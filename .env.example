# Copy to .env and run `bun scripts/gen-keys.ts` to fill the empty values.
# MODEL_KEY    — 32-byte hex, used by AES-256-GCM to encrypt/decrypt the model.
# SIGNING_KEY  — 32-byte hex, used by HMAC-SHA256 to sign /api/token responses.

MODEL_KEY=
SIGNING_KEY=
PORT=8080
NODE_ENV=production
