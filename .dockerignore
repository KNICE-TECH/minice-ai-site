node_modules
dist
.git
.github
.vscode
.idea
.DS_Store
*.log
bash.exe.stackdump
*.tsbuildinfo
.vite

# secrets must never enter the build context
.env
.env.local

# wasm intermediates (stage 1 rebuilds these)
wasm/target
src/wasm/pkg

# Raw licensed model — must NEVER enter a Docker layer. The build uses
# the committed, already-encrypted server/assets/nayu.bin instead.
assets-src
