node_modules
dist
.DS_Store
*.log
bash.exe.stackdump
*.tsbuildinfo
.vite

# secrets & build artifacts for asset protection
.env
.env.local
# Raw licensed VRM — never commit (public repo). The ENCRYPTED blob
# server/assets/nayu.bin IS committed: it's AES-256-GCM ciphertext,
# safe to publish, and the CI image build needs it.
assets-src/
src/wasm/pkg/
wasm/target/
